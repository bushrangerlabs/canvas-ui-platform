{
  "config": {
    "step": {
      "user": {
        "title": "Canvas UI Platform",
        "description": "Registers the Canvas UI Platform panel in Home Assistant. Make sure the Canvas UI Platform add-on is installed and running."
      }
    },
    "abort": {
      "single_instance_allowed": "Already configured. Only one instance is allowed."
    }
  }
}
