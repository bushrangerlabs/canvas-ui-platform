/**
 * Platform stub — visibility conditions connected to data sources (TODO).
 */
import type { VisibilityConfig } from '../types';
export function useVisibility(_config: VisibilityConfig | undefined): boolean {
  return true;
}
